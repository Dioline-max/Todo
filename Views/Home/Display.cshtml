@page
@model Todo.Pages.Display;

@* <h2> My Todos</h2>
<h2>ToDo List</h2>

<a asp-page="Create" class="btn btn-primary">Create New ToDo</a>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Completed</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var todo in Model.Todos)
        {
            <tr>
                <td>@todo.Title</td>
                <td>@todo.Description</td>
                <td>@(todo.IsComplete ? "Yes" : "No")</td>
                <td>
                    <a asp-page="EditToDo" asp-route-id="@todo.Id" class="btn btn-warning">Edit</a>
                    <a asp-page="DeleteToDo" asp-route-id="@todo.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@